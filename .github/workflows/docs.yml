name: Docs generator

on:
  release:
    types: [published]

jobs:
  push_doc:
    runs-on: ubuntu-latest
    steps:
      - uses: Bilal2453/discordia-docgen-action@main
        with:
          token: ${{ secrets.PERSONAL }}