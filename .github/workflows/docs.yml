name: Docs generator

on:
  release:
    types: [published]

jobs:
  push_doc:
    runs-on: ubuntu-latest
    steps:
      - uses: discordia-docgen-action
        with:
          token: ${{ secrets.PERSONAL }}